package main

// Regenerate the scoped chroma stylesheets (light/dark) embedded under
// internal/assets/static. Run `go generate ./...` after changing the chroma
// styles referenced in tools/genchroma.
//
//go:generate go run tools/genchroma/main.go
